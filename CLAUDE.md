# Decolit

> Общие правила (язык, ретроспективы, цикл 5 ходов, ошибка → feedback) — в `/Users/ksenia/AI/.clinerules`.
> Правила бизнеса — в `/Users/ksenia/AI/decolit/business/INDEX.md`.

## Что это

Разработка нового сайта на основе существующего. Исходный сайт-референс пользователь пришлёт отдельно — он ляжет в `raw/`. На его базе делаем новый сайт.

## Статус

Изучен сайт-референс `decolitpro.ru` (декоративные штукатурки, Москва, продажа + нанесение, премиум). Проведён аудит 4 субагентами (UX/CRO, дизайн, контент/SEO, конкуренты) — см. `analysis/2026-06-12-audit-i-napravlenie.md`.

Шаблон сайта (`site/`) свёрстан и отполирован (2026-06-12, дизайн v2): кириллические самохостинговые шрифты Prata + Golos Text, генеративные SVG-фактуры материалов, тёмный hero, тикер, манифест, рабочие фильтр галереи и счётчики. Задеплоен на Amvera: https://decolit-mozartwa.amvera.io/. Контент — демонстрационные заглушки; дальше — реальные данные от заказчика и цикл планов (`plans/`).

## Архитектура

| Файл / папка | Роль |
|---|---|
| `analysis/` | Аудит, исследования, стратегическое направление (фундамент для планов) |
| `raw/` | Исходный сайт-референс и сырьё для обработки |
| `plans/` | Технические планы фич нового сайта |
| `tmp/` | Одноразовые файлы (сверки, выгрузки), в `.gitignore` |

## Разовые файлы

`tmp/` — одноразовые файлы. Не часть системы, в `.gitignore`. Правило — в `/Users/ksenia/AI/.clinerules`, раздел «Разовые файлы (`tmp/`)».

## Планы реализации

`plans/` — технические планы фич. Правила (один план = одна фича, именование, структура фаз, цикл создания) — в `/Users/ksenia/AI/.clinerules`, раздел «Большая фича = план в `plans/`».

## Проверка качества и security review

При закрытии фазы плана `[x]` — прогнать цикл из 5 ходов (`/Users/ksenia/AI/.clinerules`). Ходы 1–4 обязательны всегда; ход 5 (security review) — если фаза касалась персданных, секретов/токенов или внешнего ввода-вывода.

Sensitive-поверхность пока не определена — уточнить, когда будет ясна архитектура сайта (формы, сбор данных, интеграции).
